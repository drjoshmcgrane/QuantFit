% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/prepare_polytomous.R
\name{prepare_polytomous}
\alias{prepare_polytomous}
\title{Prepare polytomous data for the conjoint checks}
\usage{
prepare_polytomous(resp, ss.lower = 10, cell.lower = 5, order.columns = TRUE)
}
\arguments{
\item{resp}{Integer matrix or data frame of polytomous responses (persons in
rows, items in columns), each item scored with consecutive integer
categories starting at 0. Complete data only.}

\item{ss.lower}{Only total-score groups with at least this many respondents
are used (as in \code{\link[=PrepareChecks]{PrepareChecks()}}).}

\item{cell.lower}{Minimum number of in-play respondents required in every
retained cell. Cells with fewer are pruned away; larger values give a
smaller but more stably estimated matrix.}

\item{order.columns}{If \code{TRUE} (default) the retained sub-items are ordered
by ascending overall proportion (\code{sum(n)/sum(N)}), mirroring the
difficulty ordering \code{\link[=PrepareChecks]{PrepareChecks()}} applies to items.}
}
\value{
A list with elements \code{N} and \code{n} -- the number of in-play responses
and the number scoring the higher category, for each score-group (row) by
sub-item (column) cell -- ready to pass to \code{\link[=ConjointChecks]{ConjointChecks()}} or
\code{\link[=KaraChecks]{KaraChecks()}}. The attributes \code{"dropped"} (a list of the removed score
groups and sub-items) and \code{"sub_items"} (the surviving item/step map) record
what the dense-core pruning kept.
}
\description{
Builds the \code{N} and \code{n} count matrices required by \code{\link[=ConjointChecks]{ConjointChecks()}},
\code{\link[=HiConjointChecks]{HiConjointChecks()}}, and \code{\link[=KaraChecks]{KaraChecks()}} from \emph{polytomous} response data,
via the adjacent-category recoding of \code{\link[=recode_adjacent]{recode_adjacent()}}. Each polytomous
item is split into its adjacent-category dichotomous sub-items, respondents
are grouped by their total (sum) score across the original polytomous items
-- the sufficient statistic for the partial credit model -- and the
score-by-sub-item count matrix is formed exactly as \code{\link[=PrepareChecks]{PrepareChecks()}} does
for dichotomous data.
}
\details{
Adjacent-category coding leaves some score-group-by-sub-item cells
structurally empty (a low-scoring group contains nobody at a high category
boundary, and vice versa), whereas \code{\link[=ConjointChecks]{ConjointChecks()}} requires every cell to
be populated. The score groups (rows) and sub-items (columns) are therefore
pruned to the largest dense block in which every cell has at least
\code{cell.lower} in-play respondents, iteratively dropping the row or column
carrying the most under-populated cells. This extends the sum-score-group
filtering that \code{\link[=PrepareChecks]{PrepareChecks()}} already applies through \code{ss.lower} to both
dimensions.
}
\examples{
# simulate a small partial-credit data set (4 categories, 6 items)
set.seed(1)
nP <- 800; J <- 6; M <- 3
theta <- rnorm(nP)
delta <- matrix(seq(-1, 1, length.out = J), J, M) +
  matrix(seq(-1, 1, length.out = M), J, M, byrow = TRUE)
resp <- matrix(0L, nP, J)
for (j in seq_len(J)) {
  num <- cbind(0, t(apply(outer(theta, delta[j, ], "-"), 1, cumsum)))
  P <- exp(num) / rowSums(exp(num))
  resp[, j] <- rowSums(runif(nP) > t(apply(P, 1, cumsum)))
}
pc <- prepare_polytomous(resp, ss.lower = 10, cell.lower = 3)
dim(pc$N)

}
\seealso{
\code{\link[=recode_adjacent]{recode_adjacent()}}, \code{\link[=PrepareChecks]{PrepareChecks()}}, \code{\link[=ConjointChecks]{ConjointChecks()}},
\code{\link[=KaraChecks]{KaraChecks()}}
}
