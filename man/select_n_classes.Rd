% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/select_n_classes.R
\name{select_n_classes}
\alias{select_n_classes}
\title{Select the number of latent classes by information criterion}
\usage{
select_n_classes(
  data,
  C_range = 1:6,
  n_starts = 5,
  criterion = c("BIC", "AIC"),
  use_cpp = TRUE,
  mc.cores = 1L,
  seed = NULL,
  verbose = FALSE,
  ...
)
}
\arguments{
\item{data}{Binary response matrix (persons x items).}

\item{C_range}{Integer vector of class counts to consider (default
\code{1:6}). \code{C = 1} is the single-class independence baseline (a homogeneous
population with no latent heterogeneity); \code{C >= 2} are fitted with
\code{\link[=fit_un]{fit_un()}}.}

\item{n_starts}{Number of random starts for each unconstrained fit
(default 5).}

\item{criterion}{Information criterion used to pick the class count,
\code{"BIC"} (default) or \code{"AIC"}.}

\item{use_cpp}{Use the compiled C++ EM engine (default TRUE).}

\item{mc.cores}{Number of cores. The class counts are fitted concurrently
with \code{\link[parallel:mclapply]{parallel::mclapply()}} when \code{mc.cores > 1} on a non-Windows
platform; each fit is seeded independently so the result is identical to
the serial run.}

\item{seed}{Optional integer seed; fit for class count \code{C} uses
\code{seed + C}, making the whole enumeration reproducible.}

\item{verbose}{Print progress (default FALSE).}

\item{...}{Further arguments passed to \code{\link[=fit_un]{fit_un()}}.}
}
\value{
An object of class \code{qlnclasses}: a list with
\describe{
\item{table}{Data frame with one row per class count and columns
\code{C}, \code{loglik}, \code{n_par}, \code{AIC}, \code{BIC}, \code{entropy} (normalised
classification entropy, \code{NA} for \code{C = 1}), and \code{converged}.}
\item{best_C}{The class count minimising \code{criterion}.}
\item{criterion}{The criterion used.}
\item{fits}{Named list of the fitted \code{qlfit} objects (\code{NULL} for
\code{C = 1} and for any fit that failed), keyed by class count.}
}
}
\description{
Fits the unconstrained latent class model across a range of class counts
and returns an enumeration table together with the class count preferred
by the chosen information criterion. This is the standard first stage of a
two-stage workflow: decide \emph{how many} classes the data support, then decide
\emph{what structure} those classes have with \code{\link[=select_model_ll]{select_model_ll()}} or
\code{\link[=compare_models]{compare_models()}}.
}
\details{
The unconstrained model is used for enumeration because it is the least
restrictive of the latent class models and the natural reference for "how
many groups are there". Its parameter count is \code{(C - 1) + C * J}, so BIC
genuinely rewards parsimony across class counts (unlike the \emph{structural}
comparison at a fixed \code{C}, where UN, MON, IIO and DM share a parameter
count). The single-class model (\code{C = 1}) has each item at its sample
proportion and \code{J} parameters; comparing it against \code{C >= 2} tests whether
there is any latent heterogeneity at all.

A low \code{best_C} (especially \code{C = 1}) is itself informative: it indicates the
data support little or no class structure, and any subsequent structural
comparison should be read in that light.
}
\examples{
\dontrun{
set.seed(1)
theta <- rnorm(600)
beta <- seq(-1.5, 1.5, length.out = 8)
dat <- matrix(rbinom(600 * 8, 1, plogis(outer(theta, beta, "-"))), 600, 8)
nc <- select_n_classes(dat, C_range = 1:6, mc.cores = 2, seed = 1)
nc
}
}
\seealso{
\code{\link[=select_model_ll]{select_model_ll()}} (which accepts a range for its \code{n_classes}
argument and calls this function internally), \code{\link[=fit_un]{fit_un()}},
\code{\link[=entropy_r2]{entropy_r2()}}.
}
