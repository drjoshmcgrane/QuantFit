% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/select_ll.R
\name{rm_vs_un_test}
\alias{rm_vs_un_test}
\title{Calibrated RM-vs-UN adequacy test (continuous quantitative edge)}
\usage{
rm_vs_un_test(
  data,
  fit_rm_obj,
  fit_un_obj,
  B = 99,
  alpha = 0.05,
  n_starts = 3,
  use_cpp = TRUE,
  mc.cores = 1L,
  seed = NULL
)
}
\arguments{
\item{data}{Binary response matrix.}

\item{fit_rm_obj}{Fitted RM object (from \code{\link[=fit_rm]{fit_rm()}}).}

\item{fit_un_obj}{Fitted UN qlfit at the selected class count.}

\item{B}{Bootstrap replicates (default 99).}

\item{alpha}{RM is adequate (quantitative admitted) when \code{p_value > alpha}.}

\item{n_starts}{Random starts for the UN refits.}

\item{use_cpp}{Use the compiled EM engine.}

\item{mc.cores}{Cores for the bootstrap.}

\item{seed}{Optional integer seed.}
}
\value{
A list with \code{statistic} (observed LR), \code{p_value} (P(null >= obs);
small => RM rejected), \code{null}, \code{B_effective}, \code{B_failed}, \code{available},
and \code{rm_adequate} (\code{p_value > alpha}).
}
\description{
Tests whether the continuous Rasch model (RM) fits as well as the fully
unconstrained model, by locating the observed likelihood-ratio statistic
2*(logLik_UN - logLik_RM) in a parametric-bootstrap null simulated from the
fitted RM. This makes RM a first-class citizen in the selection lattice:
quantitative structure can be admitted through the CONTINUOUS model
directly, not only through the discrete latent-class Rasch (LCR) gate.
}
\details{
Unlike a raw BIC comparison - which RM's extreme parsimony (J + 1
parameters) wins even on genuinely ordinal data - the bootstrap null
calibrates how large the UN-vs-RM likelihood gap can be \emph{when RM is true},
so true-DM/IIO data (whose UN fit captures real structure RM cannot) yield
an observed gap in the null's upper tail and RM is correctly rejected.
}
\keyword{internal}
