% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/kara_bootstrap_null.R
\name{omni_bootstrap_null}
\alias{omni_bootstrap_null}
\alias{kara_bootstrap_null}
\title{Omnibus cancellation-hierarchy test (Karabatsos KL, bootstrapped null)}
\usage{
omni_bootstrap_null(
  data,
  n_bands = 6L,
  B = 99,
  cutoff = 0.95,
  alpha = 0.05,
  S = 10000,
  N_synth = 100,
  latent = c("empirical", "normal"),
  person_order = c("complete", "facility", "adjusted"),
  propagate_item_error = FALSE,
  mc.cores = 1L,
  seed = NULL,
  verbose = TRUE
)

kara_bootstrap_null(...)
}
\arguments{
\item{data}{Binary response matrix (persons x items).}

\item{n_bands}{Maximum number of score bands (default 6).}

\item{B}{Number of Rasch-simulated null datasets (default 50).}

\item{cutoff}{Null percentile above which interval scaling is rejected
(default 0.95).}

\item{alpha}{Significance level: rejection is \code{p_value <= alpha}. B bounds
the smallest attainable p at 1/(B+1); the default B = 99 supports 5\%
decisions (B = 12-50 economy configs cannot reject at 0.05 or do so
coarsely - treat their output as descriptive percentiles).}

\item{S, N_synth}{Iterations and synthetic datasets for each \code{\link[=KaraChecks]{KaraChecks()}}
run (defaults 10000, 100), used identically for the observed and null.}

\item{latent}{How person abilities are drawn in the null replicates:
\code{"empirical"} (default) samples from the latent distribution estimated
from the data, \code{"normal"} draws theta ~ N(0, sigma^2). See
\code{\link[=cc_bootstrap_null]{cc_bootstrap_null()}} for the rationale.}

\item{person_order}{Person ordering used for score banding when responses
are missing; see \code{\link[=band_by_score]{band_by_score()}}. \code{"complete"} (default) bands complete
cases only; \code{"facility"} and \code{"adjusted"} keep all persons at the cost of
an extra-ordinal commensuration assumption. Complete data are identical
under all three.}

\item{propagate_item_error}{As in \code{\link[=cc_bootstrap_null]{cc_bootstrap_null()}}: bootstrap the item
difficulties per replicate instead of treating plug-ins as exact.}

\item{mc.cores}{Cores for the bootstrap (default 1); replicates are seeded
independently so parallel and serial results agree.}

\item{seed}{Optional integer seed.}

\item{verbose}{Print progress (default TRUE).}

\item{...}{Arguments passed to \code{\link[=omni_bootstrap_null]{omni_bootstrap_null()}}.}
}
\value{
An object of class \code{ccnull} (shared with \code{\link[=cc_bootstrap_null]{cc_bootstrap_null()}}):
\code{observed} (global KL), \code{null}, \code{percentile}, \code{p_value}, \code{reject}, and
settings. Its \code{check} field is \code{"omni-KL"}.
}
\description{
Calibrates the Karabatsos (2018) global Kullback-Leibler additivity
statistic against a null distribution simulated from the Rasch model fitted
to the data - the same per-dataset parametric-bootstrap logic that Student
& Read (2025) apply to the \code{\link[=ConjointChecks]{ConjointChecks()}} violation rate (see
\code{\link[=cc_bootstrap_null]{cc_bootstrap_null()}}). Karabatsos's fixed KL > 0.01 per-cell criterion is a
rule of thumb whose relationship to sampling variability depends on sample
size, test length, and item parameters; locating the observed global KL in
a Rasch null distribution gives an interpretable, per-dataset percentile
p-value instead.
}
\details{
The observed global KL is computed on a sum-score-banded matrix (see
\code{\link[=band_by_score]{band_by_score()}} - conditioning is on the observable score ordering, never
on a fitted ability); \code{B} datasets are then simulated under the fitted
\emph{marginal} Rasch model - abilities redrawn from N(0, sigma^2) per replicate,
with the estimated item difficulties - each is re-fitted, re-banded, and
passed through \code{\link[=KaraChecks]{KaraChecks()}} at the same \code{S}, and the observed global KL is
located in the resulting null distribution. Observed and null share the
identical pipeline and iteration count, so any baseline the pipeline induces
on additive data cancels. Interval scaling is rejected when the observed
global KL exceeds the \code{cutoff} percentile of the null.

This is computationally heavy - each replicate is a full \code{\link[=KaraChecks]{KaraChecks()}}
importance-sampling run - so \code{B} and \code{S} default lower than for the
cheaper CC bootstrap; increase them for a final analysis.
}
\examples{
\dontrun{
set.seed(1)
theta <- rnorm(1000); beta <- seq(-2, 2, length.out = 20)
dat <- matrix(rbinom(1000 * 20, 1, plogis(outer(theta, beta, "-"))), 1000, 20)
kara_bootstrap_null(dat, B = 40, mc.cores = 4, seed = 1)
}
}
\references{
Karabatsos, G. (2018). On Bayesian testing of additive conjoint measurement
axioms using synthetic likelihood. \emph{Psychometrika}, 83(2), 321-332.

Student, S. R., & Read, W. S. (2025). Applying Bayesian checks of
cancellation axioms for interval scaling in limited samples.
\emph{Behavior Research Methods}, 57, 305.
}
\seealso{
\code{\link[=cc_bootstrap_null]{cc_bootstrap_null()}}, \code{\link[=KaraChecks]{KaraChecks()}}, \code{\link[=quant_fit]{quant_fit()}}.
}
