% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/select_manifest.R
\name{select_model_manifest}
\alias{select_model_manifest}
\title{Manifest-2x2 latent-structure selector (property-based ordinal layer)}
\usage{
select_model_manifest(
  data,
  n_classes = 3L,
  B = 49L,
  n_starts = 5L,
  mon_eps = 0.03,
  alpha = 0.05,
  cc_B = 99L,
  cc_n_mat = 500L,
  use_cpp = TRUE,
  mc.cores = 1L,
  seed = NULL,
  verbose = FALSE
)
}
\arguments{
\item{data}{Binary response matrix (persons x items).}

\item{n_classes}{Number of latent classes (or a range; the median is used
for the constraint fits). Default 3.}

\item{B}{Bootstrap replicates per axis and per quantitative edge (default 49).}

\item{n_starts}{Random starts for the constraint fits (default 5).}

\item{mon_eps}{Negligible class-probability decrease below which monotonicity
is treated as holding (default 0.03).}

\item{alpha}{Significance level for the quantitative edges (default 0.05).}

\item{use_cpp}{Use the compiled EM engine.}

\item{mc.cores}{Cores for the bootstraps.}

\item{seed}{Optional integer seed.}

\item{verbose}{Print progress.}
}
\value{
A list with \code{selected}, \code{interpretation}, \code{scale}, and the axis /
edge evidence (\code{iio}, \code{mon}, and, when reached, \code{rm_vs_lcr}).
}
\description{
An alternative to \code{\link[=select_model_ll]{select_model_ll()}} that decides the ordinal / nominal
layer (UN, MON, IIO, DM) by testing the invariant-item-ordering and
class-monotonicity PROPERTIES directly against the data, then enters the
quantitative sequence DM -> LCR -> RM. On simulated data this roughly
doubles IIO recovery relative to the LR-edge lattice (which loses power when
DM and IIO coincide) and is far cheaper. Kept as a SEPARATE selector; it
does not change \code{\link[=select_model_ll]{select_model_ll()}}.
}
\seealso{
\code{\link[=select_model_ll]{select_model_ll()}} for the LR-edge lattice.
}
