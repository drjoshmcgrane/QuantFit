% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/TripleCancel.R
\name{TripleCancel}
\alias{TripleCancel}
\title{Check triple cancellation in a sample of 4-matrices}
\usage{
TripleCancel(N, n, n.4mat = 1, CR = c(0.025, 0.975), mc.cores = 1)
}
\arguments{
\item{N}{Matrix containing the total number of responses.}

\item{n}{Matrix containing the number of correct responses.}

\item{n.4mat}{Number of 4-matrices to sample or the string \code{"adjacent"}
if all adjacently formed 4-matrices are to be checked.}

\item{CR}{Width of the credible region taken from the posterior. Defaults
to a 95\% credible region (\code{c(.025,.975)}).}

\item{mc.cores}{The number of cores to parallelize over.}
}
\value{
An object of class \code{\link[=checks-class]{checks}} summarizing the
detected violations.
}
\description{
Checks ONLY the triple cancellation axiom of additive conjoint measurement
in a sample of 4x4 submatrices of \code{n}/\code{N}. The check uses the 14 coherent
triple cancellation tests within a 4x4 matrix.
}
\details{
Note that this standalone function deliberately tests the triple
cancellation axiom IN ISOLATION: the sampler drops the single-cancellation
(row/column monotonicity) bounds. This differs from
\code{\link[=ConjointChecks]{ConjointChecks()}} with \code{check="triple"}, which imposes the single,
double, and triple cancellation axioms jointly. Results from the two
approaches are therefore not comparable.
}
\examples{
\dontrun{
# simulated Rasch example
n.items <- 20
n.respondents <- 2000
diff <- rnorm(n.items)
abil <- rnorm(n.respondents)
kern <- outer(abil, diff, "-")
pv <- exp(kern)/(1+exp(kern))
resp <- ifelse(pv > runif(n.items*n.respondents), 1, 0)
tmp <- PrepareChecks(resp)
out <- TripleCancel(tmp$N, tmp$n, n.4mat = 100)
summary(out)
}

}
\references{
Perline, R., Wright, B. D., & Wainer, H. (1979). The Rasch model as
additive conjoint measurement. \emph{Applied Psychological Measurement},
3(2), 237-255.
}
\seealso{
\code{\link[=ConjointChecks]{ConjointChecks()}}, \code{\link[=SingleCancel]{SingleCancel()}}, \code{\link[=DoubleCancel]{DoubleCancel()}},
\code{\link[=HiConjointChecks]{HiConjointChecks()}}
}
\author{
Ben Domingue \email{ben.domingue@gmail.com}
}
