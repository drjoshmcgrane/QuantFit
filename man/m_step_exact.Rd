% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/em_algorithm.R
\name{m_step_exact}
\alias{m_step_exact}
\title{Exact constrained M-step via weighted isotonic regression}
\usage{
m_step_exact(data, posteriors, constraints_spec, item_order = NULL)
}
\arguments{
\item{data}{Binary data matrix}

\item{posteriors}{Posterior class memberships (n x C)}

\item{constraints_spec}{A ql_constraints object}

\item{item_order}{Item order vector (needed for item ordering constraints)}
}
\value{
List with updated item_probs, class_probs, degenerate flag
}
\description{
The constrained M-step maximizes
\eqn{Q(p) = \sum_c N_c [\bar{p}_{ic} \log p_{ic} +
(1-\bar{p}_{ic}) \log(1-p_{ic})]} where \eqn{N_c} are the expected class
counts and \eqn{\bar{p}_{ic}} the unconstrained weighted means. By the
exponential-family isotonic MLE theorem (Robertson, Wright & Dykstra),
the constrained maximizer equals the weighted L2 isotonic regression of
\eqn{\bar{p}} with weights \eqn{N_c}, computed by
\code{\link{project_constraints_weighted}}. This M-step is exact, so the
EM retains its monotone ascent property.
}
\keyword{internal}
