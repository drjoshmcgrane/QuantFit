% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate_responses.R
\name{simulate_responses}
\alias{simulate_responses}
\title{Simulate item responses from a specified latent-structure model}
\usage{
simulate_responses(
  model = c("UN", "MON", "IIO", "DM", "LCR", "RM", "PO", "PO_ITEMS"),
  n_persons = 500,
  n_items = 10,
  n_classes = 3,
  n_cat = 2L,
  class_probs = NULL,
  poset = "V",
  po_margin = NULL,
  item_order = c("free", "invariant"),
  seed = NULL
)
}
\arguments{
\item{model}{One of \code{"UN"}, \code{"MON"}, \code{"IIO"}, \code{"DM"}, \code{"LCR"}, \code{"RM"},
\code{"PO"} (partial class order; see the dedicated section), or \code{"PO_ITEMS"}
(the mirror: classes totally ordered - MON holds exactly - while ITEMS are
partially ordered; \code{poset} is then over items, keywords \code{"layers2"} /
\code{"single"} or a J x J dominance matrix; routes to the MON cell, where the
item-poset refinement must catch what the six-model verdict cannot say).}

\item{n_persons}{Number of respondents.}

\item{n_items}{Number of items.}

\item{n_classes}{Number of latent classes (ignored for \code{"RM"}).}

\item{n_cat}{Number of ordered response categories (2 = dichotomous).}

\item{class_probs}{Optional class mixing proportions (length \code{n_classes});
defaults to equal.}

\item{poset}{For \code{model = "PO"} only: the class partial order. Either a
keyword - \code{"V"} (one maximal class dominates all others, which are mutually
incomparable; the default), \code{"Lambda"} (all classes dominate one minimal
class), \code{"single"} (class 1 dominates class 2; everything else
incomparable) - or a \verb{n_classes x n_classes} logical matrix \code{D} with
\code{D[a, b] = TRUE} meaning class \code{a} dominates class \code{b} (must be
irreflexive and acyclic; transitive closure is taken). Must be neither a
chain (use \code{"MON"}) nor an antichain (use \code{"UN"}).}

\item{po_margin}{Minimum crossing depth, on the probability scale, required
of every incomparable pair in both directions. An EXPLICIT value is a
hard requirement: generation STOPS if it cannot be met (no silent
shortfall). \code{NULL} (default) means: \code{"PO"} uses 0.05 (always achievable
by its rejection loop); \code{"PO"} with \code{item_order = "invariant"} targets
0.12 and ACCEPTS its constructive best (recorded in
\code{params$po_achieved}); \code{"PO_ITEMS"} accepts the constructive maximum for
the geometry (also recorded) - its ceiling shrinks with items per level,
so explicit requests at long tests typically error with the feasible
maximum in the message.}

\item{item_order}{For \code{model = "PO"} only: \code{"free"} (default) leaves the item
side unconstrained, routing to the UN cell; \code{"invariant"} builds class
lines over one shared sorted item spine (\verb{L = a_c s_j + b_c}, \code{a_c > 0}),
so invariant item ordering HOLDS while class dominance follows the poset -
routing to the IIO cell, where only the class-poset refinement can express
the truth.}

\item{seed}{Optional random seed.}
}
\value{
An integer matrix of responses (\code{n_persons} rows, \code{n_items} columns),
scored \verb{0..n_cat-1}, with attributes \code{"model"} and \code{"params"} (the
generating parameters and, for the mixture models, the class memberships).
}
\description{
Generates dichotomous or polytomous item response data from any of the six
models in the Torres Irribarra & Diakow hierarchy, for validation and power
studies. The dichotomous case reproduces the generators of the original paper
(random logits on \eqn{U(-4, 4)}, sorted across classes for class
monotonicity, across items for invariant item ordering, or both for double
monotonicity; a partial-credit/Rasch structure for the quantitative models),
and the polytomous case generalises them through a cumulative-threshold
(for UN/MON/IIO/DM) or partial-credit (for LCR/RM) formulation.
}
\details{
For the non-parametric models each item-by-class combination is given a
location \eqn{L_{cj}} and the category structure is built from shared,
increasing threshold offsets, so that
\eqn{P(X \ge k \mid c, j) = \mathrm{logit}^{-1}(L_{cj} - \tau_k)}:
\describe{
\item{UN}{\eqn{L} left unsorted (class and item profiles may cross).}
\item{MON}{each column of \eqn{L} sorted, so category distributions are
stochastically ordered across classes.}
\item{IIO}{each row of \eqn{L} sorted, so expected item scores share one
ordering across classes.}
\item{DM}{both, so the data are doubly monotone but not necessarily
partial-credit.}
}
The quantitative models use the partial credit model
\eqn{P(X = x \mid \theta, j) \propto \exp\{\sum_{l \le x}(\theta - \delta_{jl})\}}
with discrete, separated class locations (LCR) or a continuous
\eqn{\theta \sim N(0, 1)} (RM). When \code{n_cat = 2} every model reduces to the
dichotomous generator.
}
\section{The partial-order model (\code{"PO"})}{

TI&D's generators are one \eqn{C \times J} matrix of \eqn{U(-4,4)} logits
with each column (item) fully sorted for MON and left unsorted for UN.
Sorting a column imposes the CHAIN (total class order); not sorting is the
ANTICHAIN. \code{"PO"} is the natural interpolation within the same idiom: each
column's values are sorted descending and assigned to classes along a
\strong{random linear extension of a specified partial order}, drawn independently
per item. Whenever \eqn{a \succeq b} in the poset, \eqn{a} precedes \eqn{b}
in \emph{every} linear extension, so \eqn{L_{aj} \ge L_{bj}} for all items -
\eqn{a} dominates \eqn{b} exactly. Incomparable pairs receive random relative
order per item and therefore cross (a rejection loop - the same device TI&D
use for LCR class separation - guarantees each incomparable pair crosses in
both directions by at least \code{po_margin} on the probability scale). The chain
poset reproduces \code{"MON"} exactly and the antichain reproduces \code{"UN"}, so
\code{"PO"} nests both endpoints. The item side is left unconstrained (as in UN
and MON), so the class structure is the only signal: the resulting data
violate class monotonicity (a crossing pair exists) yet carry real dominance
structure that the six-model set cannot express - the ground truth for
testing the partial-order refinement of \code{\link[=select_model_hybrid]{select_model_hybrid()}}.
}

\examples{
# doubly monotone dichotomous data, 3 classes
d <- simulate_responses("DM", n_persons = 300, n_items = 8, n_classes = 3,
                        seed = 1)
# partial-credit (Rasch) data with 4 categories
p <- simulate_responses("RM", n_persons = 300, n_items = 6, n_cat = 4,
                        seed = 1)

}
\seealso{
\code{\link[=select_model_ll]{select_model_ll()}}, \code{\link[=quant_fit]{quant_fit()}}
}
