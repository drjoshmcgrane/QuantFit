% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ManyBands.R
\name{ManyBands}
\alias{ManyBands}
\title{Check what ordering to use}
\usage{
ManyBands(
  th,
  se,
  cc.type,
  resp,
  bands = seq(10, 50, by = 10),
  uniform.bands = TRUE,
  trim.window = NULL,
  pv.order = TRUE,
  mc.cores = 1
)
}
\arguments{
\item{th}{Vector of ability estimates (thresholds).}

\item{se}{Standard errors of the ability estimates.}

\item{cc.type}{Type of cancellation check; passed to the \code{n.mat} argument
of \code{\link[=ConjointChecks]{ConjointChecks()}}.}

\item{resp}{Raw dichotomously coded response data.}

\item{bands}{Candidate numbers of ability bands.}

\item{uniform.bands}{Should the bands be uniformly spaced (\code{TRUE}) or
based on quantiles of the ability distribution (\code{FALSE})?}

\item{trim.window}{If non-\code{NULL}, respondents whose ability estimate is
within \code{trim.window} standard deviations of a band boundary are removed.}

\item{pv.order}{Use the p-value ordering of the items.}

\item{mc.cores}{The number of cores to parallelize over.}
}
\value{
A data frame with one row per candidate banding, containing the
number of bands, the trim ratio, the unweighted and weighted violation
proportions, and the stringency of the banding.
}
\description{
Internal function. This checks to see whether the p-value (Rasch
difficulty) ordering should be used or if ordering should be 'as is'.
For each candidate number of ability bands, respondents are grouped into
bands, \code{\link[=ConjointChecks]{ConjointChecks()}} is run on the resulting table, and the
proportion of violations is recorded alongside a stringency measure of
the banding.
}
\references{
Perline, R., Wright, B. D., & Wainer, H. (1979). The Rasch model as
additive conjoint measurement. \emph{Applied Psychological Measurement},
3(2), 237-255.
}
\author{
Ben Domingue \email{ben.domingue@gmail.com}
}
\keyword{internal}
