% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/HiConjointChecks.R
\name{HiConjointChecks}
\alias{HiConjointChecks}
\title{Hierarchical cancellation checks in 4x4 submatrices}
\usage{
HiConjointChecks(
  N,
  n,
  n.mat = 1,
  CR = c(0.025, 0.975),
  mc.cores = 1,
  use_cpp = TRUE,
  adjust_extremes = FALSE
)
}
\arguments{
\item{N}{Matrix containing the total number of responses.}

\item{n}{Matrix containing the number of correct responses.}

\item{n.mat}{Number of 4x4 submatrices to sample or the string
\code{"adjacent"} if all adjacently formed 4x4 submatrices are to be checked.}

\item{CR}{Width of the credible region taken from the posterior. Defaults
to a 95\% credible region (\code{c(.025,.975)}).}

\item{mc.cores}{The number of cores to parallelize over.}

\item{use_cpp}{Use the compiled C++ sampler (\code{TRUE}, the default) or the
pure R implementation.}

\item{adjust_extremes}{Nudge cells with observed proportions of exactly 0
or 1 slightly inward so that submatrices containing them can still be
checked. If \code{FALSE} (the default), such submatrices are skipped.}
}
\value{
A list with components:
\describe{
\item{\code{N}, \code{n}}{The input matrices.}
\item{\code{summary}}{A list giving the number of submatrices tested and the
counts with status \code{PASSED_ALL}, \code{FAILED_SINGLE}, \code{FAILED_DOUBLE},
\code{FAILED_TRIPLE}, \code{SKIPPED}, and \code{ERROR} (a child test that failed
with an error is reported with status \code{ERROR} rather than aborting
the whole run).}
\item{\code{results}}{A list with one element per tested submatrix containing
its rows, columns, status, and the detailed results of each stage of
the hierarchical test.}
}
}
\description{
Performs a hierarchical sequence of cancellation checks in sampled (or all
adjacent) 4x4 submatrices of \code{n}/\code{N}. Within each 4x4 submatrix the
function first tests single cancellation on the full 4x4 matrix; if that
passes, it tests double cancellation in each of the 16 embedded 3x3
submatrices; if all of those pass, it tests triple cancellation on the
full 4x4 matrix. Each submatrix is thus classified according to the
highest cancellation condition it satisfies.
}
\examples{
\dontrun{
# simulated Rasch example
n.items <- 20
n.respondents <- 2000
diff <- rnorm(n.items)
abil <- rnorm(n.respondents)
kern <- outer(abil, diff, "-")
pv <- exp(kern)/(1+exp(kern))
resp <- ifelse(pv > runif(n.items*n.respondents), 1, 0)
tmp <- PrepareChecks(resp)
out <- HiConjointChecks(tmp$N, tmp$n, n.mat = 10)
out$summary
}

}
\references{
Domingue, B. (2014). Evaluating the equal-interval hypothesis with test
score scales. \emph{Psychometrika}, 79(1), 1-19.
\doi{10.1007/s11336-013-9342-4}

Perline, R., Wright, B. D., & Wainer, H. (1979). The Rasch model as
additive conjoint measurement. \emph{Applied Psychological Measurement},
3(2), 237-255.
}
\seealso{
\code{\link[=ConjointChecks]{ConjointChecks()}}, \code{\link[=TripleCancel]{TripleCancel()}}, \code{\link[=PrepareChecks]{PrepareChecks()}}
}
