% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/select_ll.R
\name{ll_general_null}
\alias{ll_general_null}
\title{Reference distribution of the LR statistic under the GENERAL model}
\usage{
ll_general_null(
  data,
  fit_constrained,
  fit_general,
  B = 99,
  n_starts = 3,
  seed = NULL,
  use_cpp = TRUE,
  mc.cores = 1L
)
}
\arguments{
\item{data}{Response matrix.}

\item{fit_constrained, fit_general}{Fitted qlfit objects (the LR is
\eqn{2(\ell_g - \ell_c)} refitted on data simulated from \code{fit_general}).}

\item{B, n_starts, seed, use_cpp, mc.cores}{As in \code{\link[=ll_equivalence_test]{ll_equivalence_test()}}.}
}
\value{
Sorted numeric vector of LR draws (length <= B; failures dropped),
or \code{NULL} if every replicate failed.
}
\description{
Companion to \code{\link[=ll_equivalence_test]{ll_equivalence_test()}}: simulates from the fitted \emph{general}
model, refits both models to each replicate, and returns the resulting
distribution of the LR statistic. This estimates the LR distribution at the
fitted alternative - i.e. the test's power at the estimated departure from
the constrained family. Because the models are nested, when the constrained
model is true the unrestricted MLE lies essentially inside the constrained
set and this distribution coincides with the null (no power, negligible
effect size); when the truth is genuinely outside the constrained family it
shifts right (noncentral). \code{\link[=select_model_ll]{select_model_ll()}} uses the separation between
the two distributions as an effect-size check on rejections.
}
\keyword{internal}
