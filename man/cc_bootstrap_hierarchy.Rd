% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cc_bootstrap_null.R
\name{cc_bootstrap_hierarchy}
\alias{cc_bootstrap_hierarchy}
\title{Sequential hierarchy of calibrated cancellation checks}
\usage{
cc_bootstrap_hierarchy(
  data,
  levels = c("single", "double", "triple"),
  n.mat = 50,
  B = 100,
  cutoff = 0.95,
  ss.lower = 10,
  latent = c("empirical", "normal"),
  person_order = c("complete", "facility", "adjusted"),
  mc.cores = 1L,
  seed = NULL,
  verbose = TRUE
)
}
\arguments{
\item{data}{Response matrix (persons x items), dichotomous or polytomous.}

\item{levels}{Which levels to run, in order (default
\code{c("single", "double", "triple")}).}

\item{n.mat, B, cutoff, ss.lower, latent, mc.cores, seed, verbose}{As in
\code{\link[=cc_bootstrap_null]{cc_bootstrap_null()}}; each level uses \code{seed}, \code{seed + 1L}, \code{seed + 2L}.}
}
\value{
An object of class \code{cchier}: a list with \code{levels} (the per-level
\code{ccnull} objects actually run), \code{attribution} (\code{"none"} when every level
passes, otherwise the first rejecting level), \code{stopped_at} (the last level
run), and \code{supports_quant} (\code{TRUE} iff no tested level rejects).
}
\description{
Runs the calibrated cancellation checks in their logical order - single,
then double, then triple - stopping at the first level that rejects. The
cancellation axioms form a hierarchy: the double-cancellation condition is
only a logically distinct requirement once the orderings tested by single
cancellation hold, and triple likewise presupposes double. (Each individual
\code{\link[=cc_bootstrap_null]{cc_bootstrap_null()}} check already imposes the \emph{joint} constraint set up to
its level, so a deep check is always well-formed; what it cannot do is say
\emph{where} in the hierarchy additivity failed.) Testing sequentially therefore
adds attribution: a rejection at the double level, reached only after single
passed, is evidence against the genuinely double-cancellation part of
additive structure rather than a re-detection of an ordering failure.
}
\details{
Each level is calibrated exactly as in \code{\link[=cc_bootstrap_null]{cc_bootstrap_null()}}: the observed
violation rate is located in a null distribution from data simulated under
the Rasch (or, for polytomous data, partial credit) model fitted to \code{data},
passed through the identical pipeline at the same level.
}
\seealso{
\code{\link[=cc_bootstrap_null]{cc_bootstrap_null()}}, \code{\link[=quant_fit]{quant_fit()}}
}
