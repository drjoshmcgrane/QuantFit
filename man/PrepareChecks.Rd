% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/PrepareChecks.R
\name{PrepareChecks}
\alias{PrepareChecks}
\title{Prepare raw response data for ConjointChecks}
\usage{
PrepareChecks(
  resp,
  ss.lower = 10,
  collapse.columns = FALSE,
  person_order = c("complete", "facility", "adjusted")
)
}
\arguments{
\item{resp}{Raw dichotomously coded response data. Columns represent items
and rows represent individuals.}

\item{ss.lower}{Only sum scores that have at least this many distinct
individuals with that sum score will be used.}

\item{collapse.columns}{Sum over columns (items) that have the same total
score.}

\item{person_order}{How respondents are ordered/grouped when responses are
missing. \code{"complete"} (default) drops incomplete respondents - the
assumption-free frame. \code{"facility"} keeps everyone, grouping by
proportion correct among answered items (difficulty-blind).
\code{"adjusted"} groups by the observed count standardized against the
facility-implied expectation for the answered items (difficulty-aware,
imports a metric commensuration). Complete data are identical under all
three; groups never split tied respondents.}
}
\value{
Returns a list with elements \code{N} and \code{n}, containing respectively
the number of total responses and the number of correct responses for
each cell.
}
\description{
Takes raw dichotomously coded response data and produces the \code{n} and \code{N}
matrices required by \code{\link[=ConjointChecks]{ConjointChecks()}} and related functions. Items
(columns) are ordered by total score and respondents are grouped by sum
score; sum-score groups with fewer than \code{ss.lower} respondents are
dropped.
}
\examples{
# simulated Rasch example
n.items<-20
n.respondents<-2000
# simulate data
rnorm(n.items)->diff
rnorm(n.respondents)->abil
matrix(abil,n.respondents,n.items,byrow=FALSE)->m1
matrix(diff,n.respondents,n.items,byrow=TRUE)->m2
m1-m2 -> kern
exp(kern)/(1+exp(kern))->pv
runif(n.items*n.respondents)->test
ifelse(pv>test,1,0)->resp
# now check
PrepareChecks(resp)->obj

}
\seealso{
\code{\link[=ConjointChecks]{ConjointChecks()}}, \code{\link[=HiConjointChecks]{HiConjointChecks()}}, \code{\link[=KaraChecks]{KaraChecks()}}
}
\author{
Ben Domingue \email{ben.domingue@gmail.com}
}
